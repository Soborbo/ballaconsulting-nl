{
  "name": "ballaconsulting-nl",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "mkdir -p dist/server && touch dist/server/entry.mjs && astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.2.1",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.9",
    "astro-critters": "^2.2.1"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
